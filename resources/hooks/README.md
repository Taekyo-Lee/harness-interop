# resources/hooks/

Hook 선언 수집 폴더. 플러그인별로 `<플러그인>.hooks.json` 이름을 씁니다 — 자동 인식되는 default 경로명(`hooks.json`)을 피하고, marketplace entry 에서 명시 선언으로만 활성화하기 위함입니다.

(git 은 빈 폴더를 추적하지 않으므로, 이 파일이 placeholder 겸 안내 역할을 합니다.)
