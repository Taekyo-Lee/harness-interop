# ── harness 런타임 산출물 (이 repo 가 CC/OC 를 dogfooding — 동기화 파일·로컬 설정)
.opencode/
/.claude/settings.local.json
CLAUDE.local.md

# ── 내부 운영 노트 — publish 시 비공개 (이미 tracked 인 것은 저자가 git rm --cached 로 untrack)
CLAUDE.md
AGENTS.md
_reference/

# ── 실험용 dummy 플러그인 (배포물 아님)
/temp-*/

# ── 로그·임시·백업 파일
*.log
*.tmp
*.bak
*~

# ── OS 잡파일
.DS_Store
Thumbs.db
Desktop.ini

# ── 에디터/IDE
.vscode/
.idea/
*.swp
*.swo

# ── JS/TS 툴체인 (OC 플러그인 개발 잔재)
node_modules/
*.tsbuildinfo

# ── 비밀값 방어선 (public repo 보험)
.env
.env.*
*.pem
*.key
